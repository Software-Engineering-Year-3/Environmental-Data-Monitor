<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:pages="clr-namespace:ED_Monitor.Pages"
       x:Class="ED_Monitor.AppShell">
<TabBar>
    <ShellContent Title="Login" ContentTemplate="{DataTemplate pages:LoginPage}" Route="LoginPage" />
    <ShellContent Title="🏠 Home" ContentTemplate="{DataTemplate pages:MainPage}" Route="MainPage" />
    <ShellContent Title="📍 Sensor Map" ContentTemplate="{DataTemplate pages:SensorMapPage}" Route="SensorMapPage" />

</TabBar>
</Shell>
